<!DOCTYPE html>
html(lang="es")
    head
        include partials/header
    body.login
        .contenedor-formulario
            h1= pageName
            p Pon tu nueva contraseña

            form.caja-login(method="POST")
                .campo
                    label(for="password") Password:
                    input(type="password" name="password" placeholder="Tu password por favor")
                .campo.enviar
                    input.boton(type="submit" value="Restablecer passsword")

